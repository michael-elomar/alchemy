diff --git a/Android.mk b/Android.mk
index 7a1d933..911433a 100644
--- a/Android.mk
+++ b/Android.mk
@@ -41,6 +41,8 @@ LOCAL_CFLAGS := $(HGI_CFLAGS) \
 				$(HSTILIB_CFLAGS) \
 		        $(HORN_HSTILIBCONNECTIONHELPER_CFLAGS)
 
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
+
 LOCAL_WHOLE_STATIC_LIBRARIES := libhgibluetoothmediaphone
 
 LOCAL_STATIC_LIBRARIES :=  \
@@ -81,6 +83,7 @@ LOCAL_CFLAGS := $(HORN_TELEPHONY_CFLAGS) \
 	$(HSTILIB_CFLAGS) \
     $(HORN_HSTILIBCONNECTIONHELPER_CFLAGS)
 
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
 
 LOCAL_WHOLE_STATIC_LIBRARIES := libhgibluetooth_phone_phonebook
 
@@ -123,6 +126,8 @@ LOCAL_CFLAGS := $(HORN_VOLUMES_CFLAGS) \
 	$(HSTILIB_CFLAGS) \
 	$(HORN_HSTILIBCONNECTIONHELPER_CFLAGS)
 
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
+
 LOCAL_WHOLE_STATIC_LIBRARIES := libhgibluetooth
 
 LOCAL_STATIC_LIBRARIES :=  \
@@ -171,6 +176,8 @@ LOCAL_CFLAGS := $(HORN_VOLUMES_CFLAGS) \
 	$(HSTILIB_CFLAGS) \
 	$(HORN_HSTILIBCONNECTIONHELPER_CFLAGS)
 
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
+
 LOCAL_WHOLE_STATIC_LIBRARIES := libhgibluetooth_phone_phonebook
 
 LOCAL_STATIC_LIBRARIES :=  \
@@ -215,6 +222,8 @@ LOCAL_CFLAGS := $(HORN_NETDEVICE_CFLAGS) \
 	$(HSTILIB_CFLAGS) \
 	$(HORN_HSTILIBCONNECTIONHELPER_CFLAGS)
 
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
+
 LOCAL_STATIC_LIBRARIES :=  \
 			  libhornhsticonnectionhelper \
 			  libpalcore \
@@ -255,6 +264,7 @@ LOCAL_CFLAGS := $(HGI_CFLAGS) \
 				$(HSTILIB_CFLAGS) \
 		        $(HORN_HSTILIBCONNECTIONHELPER_CFLAGS) \
 
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
 
 LOCAL_WHOLE_STATIC_LIBRARIES := libhgibluetoothmediaphone
 
@@ -291,6 +301,7 @@ LOCAL_CFLAGS := $(HGI_CFLAGS) \
 				$(HSTILIB_CFLAGS) \
 		        $(HORN_HSTILIBCONNECTIONHELPER_CFLAGS) \
 
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
 
 LOCAL_STATIC_LIBRARIES :=  \
 
@@ -320,7 +331,7 @@ LOCAL_SRC_FILES:=  \
 LOCAL_CFLAGS := $(HORN_UTILS_CFLAGS) \
 	$(PAL_CFLAGS) \
 
-
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
 
 LOCAL_STATIC_LIBRARIES :=  \
 			  libpalutils
@@ -346,6 +357,8 @@ LOCAL_CFLAGS := $(HORN_HSTILIBCONNECTIONHELPER_CFLAGS) \
 				$(HSTILIB_CFLAGS) \
 				$(PAL_CFLAGS) \
 
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
+
 include $(BUILD_STATIC_LIBRARY)
 
 include $(CLEAR_VARS)
@@ -365,6 +378,8 @@ LOCAL_CFLAGS := $(HORN_HSTILIBCONNECTIONHELPER_CFLAGS) \
 				$(HSTILIB_CFLAGS) \
 				$(PAL_CFLAGS) \
 
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
+
 LOCAL_STATIC_LIBRARIES :=  \
 			libhornhsticonnectionhelper \
 			libhsti \
