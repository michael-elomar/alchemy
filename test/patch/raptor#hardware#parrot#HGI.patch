diff --git a/Build/Android/Android.mk b/Build/Android/Android.mk
index 243cc37..8be8201 100644
--- a/Build/Android/Android.mk
+++ b/Build/Android/Android.mk
@@ -73,6 +73,8 @@ LOCAL_SRC_FILES:= $(HGI_SRC_FILES)
 LOCAL_CFLAGS := $(HGI_CFLAGS) \
 		-DHGI_SUPPORT_BLUETOOTH 
 
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
+
 include $(BUILD_STATIC_LIBRARY)
 
 
@@ -90,7 +92,9 @@ LOCAL_CFLAGS := $(HGI_CFLAGS) \
 		-DHGI_SUPPORT_BLUETOOTH \
 		-DHGI_SUPPORT_MEDIA \
 		-DHGI_SUPPORT_PHONE
-		
+
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
+
 include $(BUILD_STATIC_LIBRARY)
 
 
@@ -106,6 +110,7 @@ LOCAL_SRC_FILES:= $(HGI_SRC_FILES)
 LOCAL_CFLAGS := $(HGI_CFLAGS) \
 		-DHGI_SUPPORT_PHONE 
 
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
 
 include $(BUILD_STATIC_LIBRARY)
 
@@ -121,6 +126,7 @@ LOCAL_SRC_FILES:= $(HGI_SRC_FILES)
 LOCAL_CFLAGS := $(HGI_CFLAGS) \
 		-DHGI_SUPPORT_MEDIA 
 
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
 
 include $(BUILD_STATIC_LIBRARY)
 
@@ -136,6 +142,8 @@ LOCAL_SRC_FILES:= $(HGI_SRC_FILES)
 LOCAL_CFLAGS := $(HGI_CFLAGS) \
 		-DHGI_SUPPORT_PHONEBOOK 
 
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
+
 include $(BUILD_STATIC_LIBRARY)
 
 
@@ -151,6 +159,8 @@ LOCAL_CFLAGS := $(HGI_CFLAGS) \
 		-DHGI_SUPPORT_PHONE \
 		-DHGI_SUPPORT_BLUETOOTH
 
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
+
 include $(BUILD_STATIC_LIBRARY)
 
 
@@ -167,6 +177,8 @@ LOCAL_CFLAGS := $(HGI_CFLAGS) \
 		-DHGI_SUPPORT_BLUETOOTH \
 		-DHGI_SUPPORT_PHONEBOOK
 
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
+
 include $(BUILD_STATIC_LIBRARY)
 
 # For coverity we build a special library with everything enabled
@@ -187,6 +199,8 @@ LOCAL_CFLAGS := $(HGI_CFLAGS) \
 		-DHGI_SUPPORT_PHONEBOOK \
 		-DHGI_SUPPORT_MEDIA \
 
+LOCAL_ADDITIONAL_DEPENDENCIES += $(HSTILIB_GENERATED_DONE_FILE)
+
 include $(BUILD_STATIC_LIBRARY)
 
 endif
